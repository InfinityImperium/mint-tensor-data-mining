# Pinned from the working conda env "pyscamp" (Python 3.12.12) on 2026-07-11.
# NOTE: the previous version of this file said numpy==2.4.2, but the env that
# actually ran the experiments has numpy 1.26.4 -- pinning what demonstrably
# works. torch (2.10.0, CPU) is installed separately in the Dockerfile from
# the PyTorch CPU wheel index; do not add it here.
numpy==1.26.4
pandas==3.0.0
scipy==1.17.0
matplotlib==3.10.8
tensorly==0.9.0
kneed==0.8.5
pyscamp==4.0.1
# Required by pandas.read_hdf for the LargeST loader (NOT present in the local
# conda env -- LargeST would fail there today; added so the VM works).
tables>=3.10
